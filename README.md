# ListApp

### Instalation

- git clone ListApp
- `yarn install`

#### iOS

- `cd ios && pod install`
- `react-native start`
- `react-native run-ios`

#### Android

- `react-ntive start`
- `react-native run-android`
